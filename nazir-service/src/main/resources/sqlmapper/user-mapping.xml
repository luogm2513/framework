<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nazir.dao.user.UserDao">
	<select id="getByLoginId" resultType="com.nazir.dao.user.dataobject.UserDO">
		SELECT id as userId, login_id as loginId, name, password
        FROM user
        WHERE login_id = #{loginId}
	</select>
</mapper>